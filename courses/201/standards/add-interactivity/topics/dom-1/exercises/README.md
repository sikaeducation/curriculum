# DOM 1: Exercises

## Questions

* Describe how HTML becomes the DOM
* After HTML has become the DOM, what happens to the HTML?

## Activities

Given this HTML:

```html
```

Draw the DOM tree.

---

Given this HTML:

```html
```

Draw the DOM tree.

---

Given this HTML:

```html
```

Draw the DOM tree.
